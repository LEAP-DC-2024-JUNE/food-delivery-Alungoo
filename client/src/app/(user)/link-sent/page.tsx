import SentLink from "@/components/Auth/SentLink";
import React, { Suspense } from "react";

const SentLinkPage = () => {
  return (
    <Suspense>
      <SentLink />
    </Suspense>
  );
};

export default SentLinkPage;
